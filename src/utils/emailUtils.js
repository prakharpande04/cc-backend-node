// Simple email util placeholder — integrate nodemailer if you want
export const sendEmail = async ({ to, subject, text }) => {
  console.log(`(DEMO) sendEmail called: to=${to}, subject=${subject}`);
  // Implement actual email sending with nodemailer or external provider
};
